import { FeedLoading } from "../../components/FeedLoading.js";

/** The AI vertical's wait. See `components/FeedLoading.tsx` for what it shows and why. */
export default function Loading() {
  return <FeedLoading field="ai" />;
}
